function Functionasprop(props) {

    return (
        <div>
            <p>The function to change the state passed as Prop</p>
            <button onClick={props.propfunction}>Change State</button>
        </div>
    ) ;
    
}

export default Functionasprop;